import Navigation from "./styles";
import styled from "styled-components";

const NavBar = () => {
	return (
		<Navigation>
			<Navigation.Item item="Home">elemento</Navigation.Item>
		</Navigation>
	);
};

export default NavBar;
